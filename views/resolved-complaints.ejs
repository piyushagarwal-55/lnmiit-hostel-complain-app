<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        /* General Page Layout */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
}

header {
    background-color: #333;
    padding: 15px;
    text-align: center;
}

header nav ul {
    list-style-type: none;
    padding: 0;
}

header nav ul li {
    display: inline;
    margin: 0 15px;
}

header nav ul li a {
    color: white;
    text-decoration: none;
    font-size: 16px;
}

header nav ul li a:hover {
    text-decoration: underline;
}

/* Container for content */
.container {
    width: 80%;
    margin: 30px auto;
    background-color: white;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

/* Title */
h1 {
    text-align: center;
    color: #333;
    margin-bottom: 20px;
}

/* Table Styling */
table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
}

table, th, td {
    border: 1px solid #ddd;
}

th, td {
    padding: 12px;
    text-align: left;
}

th {
    background-color: #333;
    color: white;
}

td {
    background-color: #f9f9f9;
}

td:hover {
    background-color: #f1f1f1;
}

/* Empty State */
p {
    text-align: center;
    font-size: 18px;
    color: #777;
}

/* Footer */
footer {
    background-color: #333;
    color: white;
    text-align: center;
    padding: 10px;
    margin-top: 30px;
}

footer p {
    margin: 0;
}

/* Responsive Design */
@media (max-width: 768px) {
    .container {
        width: 95%;
    }

    table {
        font-size: 14px;
    }
}

    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resolved Complaints</title>
    <link rel="stylesheet" href="/stylesheets/style.css"> <!-- Your CSS file -->
    
</head>
<body>
     <%- include('./layout/header') %>
    <style>
        div.messages {
  display:none;
  justify-content: center;
  align-items: center;
  width: 0%;
  padding: 0rem;
}
    </style>
    <div class="container">
        <h1>Resolved Complaints</h1>
        
        <% if (complaints.length === 0) { %>
            <p>No resolved complaints at the moment.</p>
        <% } else { %>
            <table>
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Description</th>
                        <th>Status</th>
                        <!-- <th>User</th> -->
                    </tr>
                </thead>
                <tbody>
                    <% complaints.forEach(complaint => { %>
                        <tr>
                            <td><%= complaint.title %></td>
                            <td><%= complaint.description %></td>
                            <td><%= complaint.status %></td>
                          
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } %>
    </div>

    <!-- <footer>
        <p>&copy; 2025 Hostel Complaint System</p>
    </footer> -->

    <script src="app.js"></script>
</body>
</html>
